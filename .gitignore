# Node
node_modules/
npm-debug.log*
dist/
.vite/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Claude Code / AI tooling
.claude/
CLAUDE.local.md

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.local

# Per-installation runtime state, not source: which node MACs an operator has
# allowed onto this gateway's mesh (uart_bridge/peer_registry.py). Committing
# one house's MACs would push them onto every other install.
peers.json
