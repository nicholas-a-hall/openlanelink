[project]
name = "openlanelink-uart-bridge"
version = "0.1.0"
description = "Standalone UART bridge service between the gateway ESP32 and the openlanelink compute node"
requires-python = ">=3.11"
dependencies = [
    "pyserial>=3.5",
    "fastapi>=0.111",
    "uvicorn[standard]>=0.30",
]
