[project]
name = "openlanelink-vision"
version = "0.1.0"
description = "openlanelink camera-based pinfall detection daemon and calibration tool"
requires-python = ">=3.11"
dependencies = [
    "opencv-python>=4.9",
    "numpy>=1.26",
    "fastapi>=0.111",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
]
